@model IEnumerable<Software_Engineering_Assingment.Models.ElementSection>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ElementHeaderId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ElementHeaderId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ElementSectionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ElementSectionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ElementSectionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
